<tr>
  <td><%= treat.site %></td>
  <td><%= treat.username %></td>
  <td><%= treat.cookies %></td>
  <td>
    <%= link_to fa_icon('trash-o'), treat, method: :delete, data: { confirm: "Really?" } %>
  </td>
</tr>
